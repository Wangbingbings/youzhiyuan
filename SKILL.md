---
name: 幼稚园
description: "将自身儿童人物参考图转换为白底大头像。生成前先确认性别：女孩在『贴纸装饰』与『公主风』两个固定模板中二选一（真实照片质感脸部、白色描边，装饰与表情可配置）；男孩在『Y2K未来感』与『藏式坏笑』两个固定模板中二选一。当用户上传自身儿童照片并要求生成贴纸头像、手账风头像、Q版大头贴、白底头像贴纸、可爱大头照时使用。"
---

# Child Sticker Avatar

## Overview
以用户上传的自身儿童人脸参考图为基础，生成白底大头像。**按性别路由**：
- **女孩** → 两个固定模板二选一（G1 贴纸装饰 / G2 公主风）
- **男孩** → 两个固定模板二选一（Y2K未来感 / 藏式坏笑）

## 前置条件
- 用户必须上传一张自身儿童人脸参考图（清晰正面照效果最佳）
- 若无参考图，直接提示用户上传后再继续，不要用纯文生图替代
- **必须先与用户确认性别（男孩/女孩）**，确认后才能路由到对应模式

## Step 0: 性别路由（必做，不可跳过）
1. 若用户消息中已明确说"男孩/女孩"，直接路由
2. 若未说明，先询问用户："是男孩还是女孩？"
3. **女孩** → 走「女孩模式」，并让用户在模板G1（贴纸装饰）与模板G2（公主风）中**二选一**（用户未指定时主动询问"选哪个模板？"）
4. **男孩** → 走「男孩模式」，并让用户在模板A（Y2K未来感）与模板B（藏式坏笑）中**二选一**

---

# 女孩模式（真实脸部 · 双模板）

## 模板选择
女孩模式下，**先让用户从两个模板中二选一**（用户未指定时主动询问"选哪个模板？"）：
- **模板G1：贴纸装饰**（默认 · 白底描边 + 四角五角星）
- **模板G2：公主风**（钻石皇冠 + 珍珠蝴蝶结 + 淡金星芒）

> 两个模板均为真实照片质感白底大头像，差异在装饰风格主题；交付前可向用户说明所选模板。

---

## 模板G1：贴纸装饰（元素固定，不可修改）

### 可配置参数（均有默认值，用户未指定则用默认）

| 参数 | 默认值 |
|------|--------|
| 头顶道具 | 带绿叶和果柄的红苹果 |
| 发饰 | 两侧各一串彩色小花发夹（粉色、黄色、蓝色、紫色） |
| 发型 | 双丸子头（两个圆圆的发髻在头顶两侧），黑色头发 |
| 表情 | 灿烂开心的笑容露出小牙齿，粉嫩腮红，嘴唇涂抹闪亮亮片唇彩带珠光效果，圆圆的大眼睛 |
| 生成数量 | 1 张 |

用户未提及的参数一律走默认，不要反复追问。若用户要求"多张/几张变体"，默认生成 3 张，每张在可配置参数上做细微差异。

### 工作流程

#### Step 1: 读取参考图
用 Read 工具读取用户上传的本地图片，确认人物面部特征与朝向。

#### Step 2: 提取参数
从用户当前消息中提取可配置参数的自定义值。多张变体时，为每张设计略有差异的参数组合（如不同表情、不同道具颜色），保持整体风格统一。

#### Step 3: 构建 Prompt
按以下模板拼接，**固定部分不可修改**：

```
以参考图片中的人物为基准，生成白底大头像。只保留头部和颈部上方，去除所有衣服和原背景。
面部必须保持真实照片质感，严格保留参考图中人物的真实五官特征、脸型与皮肤细节，禁止卡通化、禁止Q版化。
{发型}，头顶正上方平衡地放着一个{头顶道具}。
{发饰}。
面部保持参考图的五官特征，{表情}。
白色纯色背景，头部外轮廓有一圈白色描边贴纸效果。
画面四角各装饰一个淡紫色手绘空心五角星。
贴纸装饰风格，高清细节，柔和均匀自然光，居中构图。
```

#### Step 4: 调用 ImageGen 生成
- 必须使用 **ImageGen 的图生图能力**（传入 `image` 参数），**不可**用纯文生图（不传 image），因为需要保留参考图的人物面部特征
- 参数映射：
  - `prompt`：Step 3 构建的完整提示词
  - `image`：参考图（本地路径或可访问 URL，单张即可）
  - `size`：`1024x1024`（1:1 规格，实测可用；不保证支持 2048px）
  - `quality`：`high`（保证高清细节）
  - `input_fidelity`：`0.9`（实测保真最佳：真实脸部与装饰元素兼顾）
- 每张变体调用**一次** ImageGen（工具单次生成一张），多张变体按顺序依次调用

#### Step 5: 交付结果
直接在回复中逐张交付图片，每张配一句简短说明（如"变体1：双丸子头+红苹果"）。

---

## 模板G2：公主风（元素固定，不可修改）

### 固定元素（不可修改）
- 与所有模板共用：白底、白色描边贴纸效果、真实照片质感脸部、1:1 居中构图
- 头顶正上方平衡地戴着一顶小巧精致的水晶钻石皇冠（带珠光质感）
- 两侧各一个珍珠蝴蝶结发饰
- 画面四角各装饰一枚淡金色小星星，整体带柔和珠光氛围

### 可配置参数（均有默认值，用户未指定则用默认）

| 参数 | 默认值 | 兜底规则 |
|------|--------|----------|
| 发型 | 自然长卷发，黑色头发 | 原图为短发/无长发 → 降级为「半扎发」，并告知用户 |
| 表情 | 甜美微笑，粉嫩腮红，圆圆的大眼睛 | — |
| 生成数量 | 1 张（"多张"时默认 3 张） | — |

### Prompt 骨架（固定部分不可修改，仅替换占位符）

```
以参考图片中的人物为基准，生成白底大头像。只保留头部和颈部上方，去除所有衣服和原背景。
面部必须保持真实照片质感，严格保留参考图中人物的真实五官特征、脸型与皮肤细节，禁止卡通化、禁止Q版化。
{发型}，头顶正上方平衡地戴着一顶小巧精致的水晶钻石皇冠，皇冠带珠光质感。
两侧各一个珍珠蝴蝶结发饰。
面部保持参考图的五官特征，{表情}。
白色纯色背景，头部外轮廓有一圈白色描边贴纸效果。
画面四角各装饰一枚淡金色小星星，整体带柔和珠光氛围。
高清细节，柔和均匀自然光，居中构图。
```

### 工作流程

#### Step G1: 读取参考图
用 Read 工具读取用户上传的本地图片，确认人物面部特征、朝向与发型长度。

#### Step G2: 提取参数
从用户消息中提取发型、表情的自定义值；若原图为短发/无长发，发型按兜底规则降级，并准备在交付时告知用户。

#### Step G3: 构建 Prompt
按公主风骨架拼接，仅替换 `{发型}` `{表情}` 占位符，固定部分不可修改。

#### Step G4: 调用 ImageGen 生成
参数同模板G1 Step 4（`image` + `size` 1024x1024 + `quality` high + `input_fidelity` 0.9），每张变体调用**一次** ImageGen。

#### Step G5: 交付结果
逐张交付图片，每张配一句简短说明（如"公主风变体1：长卷发+钻石皇冠"）。

---

# 男孩模式

## 模板选择
男孩模式下，**必须先让用户从两个模板中二选一**（用户未指定时主动询问"选哪个模板？"）：
- **模板A：Y2K未来感**
- **模板B：藏式坏笑**

> 注：男孩两个模板均为真实感白底大头像风格（非手账贴纸风），与女孩模板风格不同，交付前可向用户说明。

## 模板A：Y2K 未来感（元素固定，不可修改）

```
以原图小男孩的头像质感、生成白底大头像。只保留头部不要衣服和背景。
五官融合：将AI生成图的脸部特征，替换或融合为原图小男孩的五官感觉，使其更像自己。俏皮的表情，{帽子}，皮肤水嫩质感。头上戴一副有未来感的耳机。画面周围加一些Y2K元素。
发型调整：保留，更有质感。周围加一些Y2K闪星。
眼神与角度：调整为更明确地往画面右侧看，带出少量眼白，营造"偷偷看"的俏皮感。
```

- `{帽子}` 占位符规则（**帽子兜底**）：
  - 原图小男孩**戴了帽子** → 替换为「头上戴着照片中的帽子」
  - 原图**没戴帽子** → 替换为「头顶正上方平衡地放着一个带绿叶和果柄的红苹果」（默认道具兜底），并告知用户"原图没有帽子，已用红苹果替代头顶装饰"

## 模板B：藏式坏笑（元素固定，不可修改）

```
以原图小男孩的头像质感、生成白底大头像。只保留头部不要衣服和背景。
五官融合：将AI生成图的脸部特征，替换或融合为原图小男孩的五官感觉，使其更像自己。稍微坏笑一点点，耳朵上戴着绿松石镶嵌的藏式耳环，脸部加一点点雀斑，头上架一副老式墨镜，不要遮住额头。
发型调整：保留，更有质感。
眼神与角度：调整为更明确地往画面右侧看，带出少量眼白，营造"偷偷看"的俏皮感。
```

## 数量规则
- 默认生成 1 张
- 用户要求"多张/几张变体"时，默认生成 3 张；每张在**保持模板固定元素不变**的前提下，只对表情幅度、光影、角度等细节做细微差异

## 工作流程

### Step B1: 读取参考图
用 Read 工具读取用户上传的本地图片，确认人物面部特征、朝向，以及（模板A时）原图是否戴帽子。

### Step B2: 确认模板
让用户在模板A与模板B中二选一，确认后进入对应模板。

### Step B3: 构建 Prompt
按选定模板原文拼接，仅处理 `{帽子}` 占位符（见模板A的兜底规则）。模板固定元素不可修改、不可替换。

### Step B4: 调用 ImageGen 生成
- 必须使用 **ImageGen 的图生图能力**（传入 `image` 参数），不可用纯文生图
- 参数：`prompt`（Step B3 结果）、`image`（参考图）、`size` 为 `1024x1024`、`quality` 为 `high`、`input_fidelity` 为 `0.9`
- 每张变体调用**一次** ImageGen，多张按顺序依次调用

### Step B5: 交付结果
直接在回复中逐张交付图片，每张配一句简短说明（如"模板A变体1：Y2K+耳机"）。

---

# 生成后自检（所有模板通用，必做）

每次生成后、交付前，用 Read 工具查看生成结果，按模板检查**固定锚点**是否齐全：

| 模板 | 必查锚点 |
|------|---------|
| 女孩 G1 贴纸装饰 | 白底、白色描边、四角淡紫色五角星 |
| 女孩 G2 公主风 | 白底、白色描边、头顶钻石皇冠、珍珠蝴蝶结 |
| 男孩 A Y2K | 白底、头部未来感耳机、往右侧看的眼神 |
| 男孩 B 藏式 | 白底、绿松石耳环、老式墨镜、轻微坏笑 |

- 锚点齐全、面部无明显卡通化/Q版化 → 交付
- **任一锚点缺失** → 基于当前生成结果**微调 prompt 重试 1 次**（保留同一参考图与 `input_fidelity: 0.9`，只强化缺失锚点的位置与特征描述，不改动其他固定元素）
- 重试后仍缺失 → 交付，并如实告知用户哪些元素未呈现
- 每张变体独立执行上述自检

---

# 注意事项
- 女孩模式两个模板的固定元素不可随意更改（G1：白底、白色描边、四角五角星、贴纸装饰风格；G2：白底、白色描边、钻石皇冠、珍珠蝴蝶结、淡金小星星）；仅各自可配置参数可调
- 男孩模式两个模板的装饰元素（耳机、藏式耳环、墨镜、雀斑、表情、发型、眼神角度等）固定不可随意更改；仅生成数量可配置
- 模板A帽子兜底：原图无帽时换默认红苹果道具，并告知用户
- 公主风发型兜底：原图为短发/无长发时降级为半扎发，并告知用户
- 所有模板共用的固定元素（真实照片质感脸部、去衣去背景、1:1 1024x1024、居中构图）不可更改
- 若参考图非正面或人脸不清晰，提醒用户但仍可尝试生成
- 单次生成建议不超过 4 张变体，避免调用过大
- 参考图中的衣服、身体、原背景必须在生成时去除，只保留头部
